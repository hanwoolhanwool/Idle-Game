# CLAUDE.md

이 프로젝트(Unity 기반 Idle Game)에서 코드를 작성할 때 지켜야 할 규칙입니다.

## 코딩 원칙

- **객체지향 설계(OOP)**를 기반으로 코드를 작성한다.
- **SOLID 원칙**을 준수한다.
  - SRP: 각 클래스는 하나의 책임만 가진다.
  - OCP: 확장에는 열려 있고 수정에는 닫혀 있도록 설계한다.
  - LSP: 하위 타입은 상위 타입을 대체할 수 있어야 한다.
  - ISP: 필요 없는 인터페이스에 의존하지 않도록 인터페이스를 분리한다.
  - DIP: 구체 구현이 아니라 추상(인터페이스)에 의존한다.
- 실무(프로덕션) 환경에서 사용할 수 있을 만큼 **유지보수 가능한** 코드를 작성한다.

## 구조 및 품질 기준

- 불필요한 복잡성을 제거한다.
- 명확한 **책임 분리**와 효율적인 구조를 우선한다.
- 향후 **확장(extension)**을 고려해 설계한다.
- **에러 처리와 엣지 케이스**를 항상 고려한다.
- 코드는 **테스트 가능한** 구조로 작성한다.

## 커뮤니케이션

- 응답 언어는 한국어를 사용한다.
- 설명 방식은 `Teaching Mode` output style을 따른다. (`/output-style` 에서 선택)

## 기술 명세서(문서) 작성 규칙

코드 기술 명세서를 작성·갱신할 때 아래 규칙을 따른다. (포트폴리오 겸 유지보수 문서)

### 저장 위치·구성

- 모든 문서는 `docs/` 아래에 둔다.
- `docs/README.md` = **문서 허브**: 전체 아키텍처 개요 + 명세서 인덱스. 새 명세서를 추가하면 이 인덱스 표를 반드시 갱신한다.
- 기능별 상세 명세서는 `docs/specs/NN_기능명.md` (번호 접두사로 정렬, 예: `01_State_Machine.md`).

### 형식

- **한국어**로 작성하고, 설명은 `Teaching Mode` 톤(왜 이렇게 설계했는지 근거 제시)을 따른다.
- 시각화는 **GitHub 렌더링 Mermaid**(Diagram as Code)로 한다. 그림 파일을 그리지 않는다 — 다이어그램도 텍스트로 정의해 diff로 리뷰되게 한다.
- 명세서는 반드시 **실제 코드를 읽고** 작성한다. 클래스·메서드·필드명은 소스와 일치시킨다(추측 금지).

### 기능별 명세서 공통 템플릿 (이 순서를 지킨다)

1. 개요 — 시스템의 책임 한 문단
2. 요구사항·설계 목표 — 표(요구사항 → 설계적 해석)
3. 구성 요소 — 계층별 타입·역할 표
4. 구조 다이어그램 — Mermaid `classDiagram`
5. 동작 흐름 — `sequenceDiagram` / `flowchart` / `stateDiagram-v2` 중 적합한 것
6. 핵심 설계 포인트 — SOLID 매핑 표 + 하이라이트 패턴 설명
7. 엣지 케이스·에러 처리 — 표
8. 확장 여지 — "지금 만들지 않되 막지 않을 것"
9. 파일 위치 — 파일 → 경로 표

### Mermaid 문법 주의 (GitHub 렌더 안정성)

- `classDiagram`의 제네릭에 콤마를 쓰지 않는다. `Dictionary~K,V~`(깨짐) → `-name : Dictionary` 형태로 표기.
- 스테레오타입(`<<...>>`)에는 특수문자(가운뎃점 `·` 등)를 넣지 않는다. 예: `<<readonly struct>>`.
- `flowchart` 노드 레이블에 괄호·콜론이 들어가면 `"..."`로 감싼다.

### 커밋

- 문서 커밋은 코드 변경과 섞지 않고 **문서만 따로** 스테이징한다.
- 커밋 타입은 `docs`, 제목·본문은 한국어. (커밋 컨벤션은 `docs/Git_Commit_Convention.md` 참조)
